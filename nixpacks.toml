[phases.setup]
nixPkgs = ["nodejs", "pnpm", "python3", "gcc", "gnumake", "libc.dev"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run start"